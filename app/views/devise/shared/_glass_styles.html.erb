<style>
  .devise-contrast-input,
  .devise-contrast-input:focus {
    background: transparent;
    color: rgba(255, 255, 255, 0.96);
    border: 0;
    border-bottom: 3px solid rgba(255, 255, 255, 0.9);
    border-radius: 0;
    box-shadow: none;
  }

  .devise-contrast-input::placeholder {
    color: rgba(255, 255, 255, 0.96);
    opacity: 1;
  }

  .devise-glass-card {
    border-radius: 2rem;
    background: rgba(33, 37, 41, 0.74);
  }

  .devise-glass-card .card-body {
    backdrop-filter: blur(2px);
  }
</style>
