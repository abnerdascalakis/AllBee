<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #1f2937;">
  <div class="container-fluid">
    <!-- Logo / Marca -->
    <a class="navbar-brand fw-bold" href="#">🐝 AllBee</a>

    <!-- Botão toggler para mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarRightLogout" aria-controls="navbarRightLogout" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon" style="filter: invert(1);"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarRightLogout">
      <!-- Links principais -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link text-white px-3" %>
        </li>
        <li class="nav-item">
          <%= link_to "Produtos", produtos_path, class: "nav-link text-white px-3" %>
        </li>
        <li class="nav-item">
          <%= link_to "Sobre nós", "#", class: "nav-link text-white px-3" %>
        </li>
      </ul>

      <!-- Botão logout à direita -->
      <div class="d-flex ms-auto">
        <% if user_signed_in? %>
        
          <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "btn btn-outline-light rounded-pill px-3" %>
        <% else %>
          <%= link_to "Entrar", new_user_session_path, class: "btn btn-outline-light rounded-pill px-3 me-2" %>
          <%= link_to "Cadastrar", new_user_registration_path, class: "btn btn-warning text-dark rounded-pill px-3" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
